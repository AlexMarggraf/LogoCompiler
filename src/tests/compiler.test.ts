import assert from "node:assert/strict";
import { describe, it, test } from "node:test";
import { compileCode } from "../xLogo_Parser/compiler.js";

describe("Compiler", () => {
  it("should compile correctly", (t) => {
    // TODO add tests here
    t.todo()
  })
})